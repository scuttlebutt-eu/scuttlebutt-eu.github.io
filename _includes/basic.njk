<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link href="/assets/fonts/inter.css" href="stylesheet">
    <style>
    :root {
        --purple: #7325FC;
    }
    body {
      background-color: lightgray;
      margin: 0;
      display: grid;
      grid-template-rows: [top] auto [middle] auto [bottom];
      grid-template-columns: [left-extreme] auto [page-left-border] minmax(60%,800px) [page-right-border] auto [right-extreme];
      height: 100vh;
      font-family: Inter, sans-serif;
      line-height: 1.4;
    }
    a {
      color: var(--purple);
    }
    li, p {
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }
    article {
      flex: 1;
      padding: 2rem 4rem;
      background-color: white;
      box-sizing: border-box;
      position: relative;
      grid-row: top / bottom;
      grid-column: page-left-border / page-right-border;
    }
    header {
      display: grid;
      grid-template-rows: [top] auto [bottom];
      grid-template-columns: repeat(4, max-content);
      grid-gap: 30px;
    }
    header > a {
      color: var(--purple);
      font-weight: bold;
      text-decoration: none;
    }
    article > h1:first-of-type {
      font-size: 2.5rem;
      color: darkred;
    }
    article > h1, article > h2, article > h3, article > p, article > ol, article > ul {
      position: relative;
      z-index: 2;
    }
    .rect0 {
      background-color: var(--purple);
      width: 21px;
      position: absolute;
      right: -11px;
      height: 240px;
      top: 9rem;
    }
    footer {
      margin: 3rem 0;
      display: flex;
      flex-direction: row;
      align-items: center;
    }
    .circle {
      width: 100px;
      height: 100px;
      background-color: var(--purple);
      border-radius: 50px;
    }
    .square {
      width: 75px;
      height: 75px;
      background-color: paleturquoise;
      transform: translateX(-36px) translateY(0px) rotateZ(45deg);
    }
    .lines0 {
      height: 9rem;
      position: absolute;
      right: 2rem;
      top: 32rem;
      z-index: 1;
      opacity: 0.4;
    }
    </style>
  </head>
  <body>
    <article>
      <header>
        <a href="/">Home</a>
        <a href="/proposal-membership/">Membership</a>
        <a href="/">Code of Conduct</a>
        <a href="/">Contact</a>
      </header>
      <div class="rect0"></div>
      <img class="lines0" src="/assets/rect-star.svg"></img>
      {{ content | safe }}
      <footer>
        <div class="circle"></div>
        <div class="square"></div>
      {% if tags and tags.includes('important-documents') %}
        <a style="color:gray" href="{{ 'https://github.com/scuttlebutt-eu/important-documents/edit/master' + page.filePathStem + '.md' }}">Edit this page on GitHub</a>
      {% else %}
        <a style="color:gray" href="{{ 'https://github.com/scuttlebutt-eu/scuttlebutt-eu.github.io/edit/master' + page.filePathStem + '.md' }}">Edit this page on GitHub</a>
      {% endif %}
      </footer>
    </article>
  </body>
</html>
